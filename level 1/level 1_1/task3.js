// Дана строка. Выведите в консоль последний символ строки.

function task3(string) {
    for (i = 0; i < string.length; i++) {
        last_simvol = string[i];
    }
    console.log(last_simvol);
}

task3("kal")